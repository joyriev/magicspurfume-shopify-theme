{% comment %}
  FRAGRANCE CODE SUCHE POPUP - PREMIUM DESIGN
  - Funktionsfähige Suchfunktion - Jetzt richtig funktionierend
  - ERWEITERTE FUZZY-SUCHE: Findet Produkte auch bei Tippfehlern
  - Spezielle Behandlung für "code" zeigt sowohl No. 260 als auch No. 135
  - FESTE ZUORDNUNGEN mit korrekten Varianten-IDs
  - FIXIERT: Verwendet korrekte Produktbilder (_c.webp Format)
  - FIXIERT: Warenkorb bleibt nach dem Hinzufügen geöffnet
  - FIXIERT: "amani" findet jetzt sowohl Armani-Produkte als auch Katalog-Parfums
  - HARDCODED: One Million, One Million Lucky, Lady Million, Fame, Invictus, Phantom
  - HARDCODED: One Million Elixir, Invictus Victory, Pure XS, Olympea Intense, Ultraviolet
{% endcomment %}

<!-- Lade unseren kuratierten Katalog -->
<script src="{{ 'perfume-catalog-minimal.js' | asset_url }}"></script>

<style>
/* ===== PREMIUM FRAGRANCE CODE POPUP ===== */
.fc-float-wrapper {
  position: fixed;
  bottom: 30px;
  right: 30px;
  z-index: 9999;
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
}

/* ===== FLOATING BUTTON ===== */
.fc-search-toggle {
  background: #C41E3A;
  border-radius: 30px;
  padding: 14px 28px;
  cursor: pointer;
  box-shadow: 0 8px 20px rgba(196, 30, 58, 0.3);
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  transition: all 0.3s ease;
  border: none;
  min-width: 130px;
}

.fc-search-toggle:hover {
  background: #A51D36;
  transform: translateY(-3px);
  box-shadow: 0 12px 25px rgba(165, 29, 54, 0.4);
}

.fc-search-toggle svg {
  width: 20px;
  height: 20px;
  stroke: #FFFFFF;
  stroke-width: 2;
  fill: none;
}

.fc-toggle-text {
  color: #FFFFFF;
  font-size: 15px;
  font-weight: 600;
  letter-spacing: 0.5px;
  text-transform: uppercase;
}

/* ===== POPUP CONTAINER ===== */
.fc-search-popup {
  position: fixed;
  bottom: 110px;
  right: 30px;
  width: 440px;
  max-height: 80vh;
  background: #FFFFFF;
  border-radius: 24px;
  box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
  padding: 0;
  overflow-y: auto;
  transform: translateY(30px);
  opacity: 0;
  visibility: hidden;
  transition: 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  z-index: 10000;
  border: 1px solid rgba(0, 0, 0, 0.05);
}

.fc-search-popup.active {
  transform: translateY(0);
  opacity: 1;
  visibility: visible;
}

/* Premium Scrollbar */
.fc-search-popup::-webkit-scrollbar {
  width: 6px;
}
.fc-search-popup::-webkit-scrollbar-track {
  background: #F8F4F0;
  border-radius: 10px;
}
.fc-search-popup::-webkit-scrollbar-thumb {
  background: #C41E3A;
  border-radius: 10px;
}

/* ===== CLOSE BUTTON ===== */
.fc-close-btn {
  position: absolute;
  top: 20px;
  right: 20px;
  width: 36px;
  height: 36px;
  background: #F8F4F0;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  font-size: 20px;
  color: #4A3F39;
  transition: all 0.2s ease;
  z-index: 10;
  border: none;
  opacity: 0.9;
}

.fc-close-btn:hover {
  background: #EDE5DC;
  color: #C41E3A;
  transform: rotate(90deg);
}

/* ===== OFFER BANNER - PREMIUM - VERBESSERTE UI ===== */
.fc-offer-banner {
  background: linear-gradient(135deg, #FFF5EB, #FFE8DA);
  border: 2px solid #C41E3A;
  border-radius: 16px;
  padding: 16px 20px;
  margin: 20px 20px 15px 20px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  box-shadow: 0 8px 15px -5px rgba(196, 30, 58, 0.2);
  transition: all 0.3s ease;
}

.fc-offer-banner.qualified {
  background: linear-gradient(135deg, #E8F5E9, #C8E6C9);
  border-color: #2E7D32;
  box-shadow: 0 8px 15px -5px rgba(46, 125, 50, 0.2);
}

.fc-offer-left {
  display: flex;
  align-items: center;
  gap: 12px;
  flex: 1;
}

.fc-offer-icon {
  font-size: 28px;
  background: #C41E3A;
  width: 48px;
  height: 48px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  color: white;
  box-shadow: 0 5px 10px rgba(196, 30, 58, 0.3);
  transition: background 0.3s ease;
  flex-shrink: 0;
}

.fc-offer-banner.qualified .fc-offer-icon {
  background: #2E7D32;
  box-shadow: 0 5px 10px rgba(46, 125, 50, 0.3);
}

.fc-offer-text {
  flex: 1;
}

.fc-offer-text strong {
  display: block;
  font-size: 15px;
  color: #C41E3A;
  font-weight: 800;
  margin-bottom: 2px;
  letter-spacing: 0.3px;
  transition: color 0.3s ease;
  line-height: 1.3;
}

.fc-offer-banner.qualified .fc-offer-text strong {
  color: #2E7D32;
}

.fc-offer-text span {
  font-size: 12px;
  color: #4A3F39;
  font-weight: 500;
  display: block;
  line-height: 1.4;
}

.fc-offer-status {
  background: #C41E3A;
  color: white;
  font-size: 14px;
  font-weight: 700;
  padding: 6px 14px;
  border-radius: 30px;
  box-shadow: 0 4px 8px rgba(196, 30, 58, 0.2);
  transition: all 0.3s ease;
  min-width: 65px;
  text-align: center;
  white-space: nowrap;
  margin-left: 10px;
}

.fc-offer-banner.qualified .fc-offer-status {
  background: #2E7D32;
  box-shadow: 0 4px 8px rgba(46, 125, 50, 0.2);
}

/* Fortschrittsbalken - VERBESSERT */
.fc-offer-progress-container {
  padding: 0 20px 15px 20px;
}

.fc-offer-progress-bar {
  height: 6px;
  background: #EDE5DC;
  border-radius: 10px;
  overflow: hidden;
  width: 100%;
}

.fc-offer-progress-fill {
  height: 100%;
  background: #C41E3A;
  border-radius: 10px;
  width: 0%;
  transition: width 0.3s ease;
}

.fc-offer-banner.qualified .fc-offer-progress-fill {
  background: #2E7D32;
}

.fc-offer-message {
  font-size: 12px;
  color: #4A3F39;
  margin-top: 8px;
  text-align: right;
  font-weight: 500;
  padding-right: 5px;
}

/* ===== INFO SECTION ===== */
.fc-info-section {
  padding: 5px 20px 5px 20px;
}

.fc-info-label {
  font-size: 12px;
  font-weight: 600;
  color: #8A7A70;
  letter-spacing: 0.8px;
  text-transform: uppercase;
  margin-bottom: 12px;
}

/* ===== LOGO SECTION - CLICKABLE ===== */
.fc-logo-section {
  padding: 0 20px 15px 20px;
  display: flex;
  align-items: center;
  gap: 15px;
  cursor: pointer;
  transition: opacity 0.2s ease;
}

.fc-logo-section:hover {
  opacity: 0.8;
}

.fc-logo-red {
  width: 60px;
  height: 60px;
  background: #C41E3A;
  border-radius: 16px;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 8px 15px rgba(196, 30, 58, 0.25);
}

.fc-logo-fc {
  color: white;
  font-size: 28px;
  font-weight: 800;
  letter-spacing: -0.5px;
}

.fc-brand-text {
  font-size: 18px;
  font-weight: 700;
  color: #2C2420;
  text-transform: uppercase;
  letter-spacing: 0.8px;
}

/* ===== SEARCH SECTION WITH CLEAR BUTTON ===== */
.fc-search-section {
  padding: 0 20px 20px 20px;
  position: relative;
}

.fc-search-container {
  position: relative;
  border-bottom: 2px solid #EDE5DC;
  transition: border-color 0.2s ease;
}

.fc-search-container:focus-within {
  border-color: #C41E3A;
}

.fc-search-field {
  width: 100%;
  padding: 15px 45px 15px 0;
  border: none;
  font-size: 16px;
  outline: none;
  background: transparent;
  color: #2C2420;
}

.fc-search-field::placeholder {
  color: #B0A298;
  font-weight: 400;
  font-size: 15px;
}

.fc-clear-search {
  position: absolute;
  right: 0;
  top: 50%;
  transform: translateY(-50%);
  width: 36px;
  height: 36px;
  background: #F8F4F0;
  border: none;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  font-size: 18px;
  color: #8A7A70;
  padding: 0;
  transition: all 0.2s ease;
}

.fc-clear-search:hover {
  background: #EDE5DC;
  color: #C41E3A;
}

/* ===== STEPS SECTION - PREMIUM BOX ===== */
.fc-steps-section {
  padding: 0 20px 25px 20px;
}

.fc-steps-box {
  background: #FEFCF9;
  border: 1px solid #EDE5DC;
  border-radius: 24px;
  padding: 28px 20px;
  box-shadow: 0 8px 20px -10px rgba(0, 0, 0, 0.08);
}

.fc-steps-title {
  font-size: 22px;
  font-weight: 800;
  color: #2C2420;
  margin-bottom: 25px;
  line-height: 1.2;
  letter-spacing: -0.3px;
}

.fc-steps-title span {
  display: block;
  color: #C41E3A;
  font-size: 24px;
}

.fc-step-item {
  font-size: 15px;
  margin: 20px 0;
  color: #4A3F39;
  display: flex;
  align-items: flex-start;
  gap: 15px;
  line-height: 1.5;
  font-weight: 500;
}

.fc-step-number {
  display: inline-block;
  width: 28px;
  height: 28px;
  background: #C41E3A;
  color: white;
  border-radius: 8px;
  text-align: center;
  line-height: 28px;
  font-size: 15px;
  font-weight: 700;
  flex-shrink: 0;
  box-shadow: 0 4px 8px rgba(196, 30, 58, 0.2);
}

/* ===== SECTION TITLES ===== */
.fc-section-title {
  font-size: 16px;
  font-weight: 800;
  color: #C41E3A;
  margin: 20px 20px 15px 20px;
  padding-bottom: 8px;
  border-bottom: 2px solid #EDE5DC;
  text-transform: uppercase;
  letter-spacing: 0.8px;
}

.fc-subsection-title {
  font-size: 16px;
  font-weight: 700;
  color: #2C2420;
  margin: 20px 20px 15px 20px;
}

/* ===== REAL PERFUME LIST ===== */
.fc-real-perfume-item {
  background: #FFFFFF;
  padding: 15px 20px;
  margin: 0;
  border-bottom: 1px solid #F0E8E0;
  cursor: pointer;
  display: flex;
  align-items: center;
  gap: 18px;
  transition: background 0.2s ease;
}

.fc-real-perfume-item:hover {
  background: #FEF9F5;
}

.fc-real-perfume-image {
  width: 60px;
  height: 60px;
  object-fit: contain;
  border-radius: 12px;
  background: #FFFFFF;
  padding: 5px;
  border: 1px solid #EDE5DC;
}

.fc-real-perfume-info {
  flex: 1;
}

.fc-real-perfume-name {
  font-size: 16px;
  font-weight: 700;
  color: #2C2420;
  margin-bottom: 4px;
}

.fc-real-perfume-brand {
  font-size: 14px;
  color: #C41E3A;
  font-weight: 600;
  margin-bottom: 2px;
}

.fc-real-perfume-year {
  font-size: 12px;
  color: #B0A298;
}

/* ===== PRODUCT GRID ===== */
.fc-grid-2col {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 18px;
  padding: 0 20px 20px 20px;
}

.fc-product-card {
  text-align: center;
}

.fc-badge {
  background: #F8F4F0;
  padding: 6px 12px;
  border-radius: 40px;
  font-size: 10px;
  font-weight: 700;
  color: #4A3F39;
  margin-bottom: 12px;
  display: inline-block;
  text-transform: uppercase;
  letter-spacing: 0.6px;
  border: 1px solid #EDE5DC;
}

.fc-product-inner {
  background: #FFFFFF;
  border: 1px solid #EDE5DC;
  border-radius: 20px;
  padding: 18px 12px;
  transition: all 0.2s ease;
}

.fc-product-inner:hover {
  border-color: #C41E3A;
  box-shadow: 0 8px 18px -8px rgba(196, 30, 58, 0.15);
}

/* ===== ENLARGED CATALOGUE IMAGES ===== */
.fc-product-image {
  width: 100%;
  height: 150px;
  object-fit: contain;
  margin-bottom: 12px;
  filter: drop-shadow(0 6px 8px rgba(0,0,0,0.1));
}

.fc-product-title {
  font-size: 13px;
  font-weight: 700;
  margin-bottom: 5px;
  color: #2C2420;
}

.fc-product-price {
  font-size: 14px;
  font-weight: 800;
  color: #C41E3A;
  margin-bottom: 10px;
}

.fc-add-to-cart {
  width: 100%;
  background: #C41E3A;
  color: white;
  border: none;
  padding: 10px;
  border-radius: 40px;
  font-size: 12px;
  font-weight: 700;
  cursor: pointer;
  margin-bottom: 8px;
  text-transform: uppercase;
  letter-spacing: 0.6px;
  transition: background 0.2s ease;
}

.fc-add-to-cart:hover {
  background: #A51D36;
}

.fc-view-link {
  font-size: 11px;
  color: #8A7A70;
  text-decoration: underline;
  cursor: pointer;
  font-weight: 500;
  display: inline-block;
}

.fc-view-link:hover {
  color: #C41E3A;
}

/* ===== DETAILED VIEW ===== */
.fc-detailed-view {
  background: #FEFCF9;
  border-radius: 24px;
  padding: 20px;
  margin: 0 20px 20px 20px;
  border: 1px solid #EDE5DC;
}

.fc-back-btn {
  background: none;
  border: none;
  padding: 0 20px 15px 20px;
  font-size: 13px;
  color: #8A7A70;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  gap: 6px;
  text-transform: uppercase;
  letter-spacing: 0.6px;
  font-weight: 600;
}

.fc-back-btn:hover {
  color: #C41E3A;
}

.fc-back-btn:before {
  content: "←";
  font-size: 16px;
}

.fc-detailed-header {
  display: flex;
  gap: 20px;
  margin-bottom: 25px;
  padding-bottom: 20px;
  border-bottom: 1px solid #EDE5DC;
}

.fc-detailed-image {
  width: 110px;
  height: 110px;
  object-fit: contain;
  border-radius: 16px;
  background: #FFFFFF;
  padding: 10px;
  border: 1px solid #EDE5DC;
}

.fc-detailed-name {
  font-size: 20px;
  font-weight: 800;
  color: #2C2420;
  margin-bottom: 6px;
}

.fc-detailed-brand {
  font-size: 15px;
  color: #C41E3A;
  font-weight: 700;
  margin-bottom: 8px;
}

.fc-detailed-meta {
  font-size: 13px;
  color: #8A7A70;
  margin-top: 5px;
}

/* ===== NOTES ===== */
.fc-notes-section {
  margin: 20px 0;
}

.fc-note-category {
  margin-bottom: 18px;
}

.fc-note-category h4 {
  font-size: 13px;
  font-weight: 800;
  color: #C41E3A;
  margin-bottom: 10px;
  text-transform: uppercase;
  letter-spacing: 0.8px;
}

.fc-note-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}

.fc-note-tag {
  background: #FFFFFF;
  border: 1px solid #EDE5DC;
  padding: 6px 14px;
  border-radius: 40px;
  font-size: 12px;
  color: #4A3F39;
  font-weight: 500;
}

.fc-description {
  margin: 20px 0 0 0;
  padding: 18px;
  background: #FFFFFF;
  border-radius: 16px;
  border: 1px solid #EDE5DC;
  font-size: 13px;
  line-height: 1.6;
  color: #4A3F39;
}

/* ===== NOTIFICATION ===== */
.fc-notification {
  position: fixed;
  top: 20px;
  right: 20px;
  background: #2E7D32;
  color: white;
  padding: 12px 24px;
  border-radius: 40px;
  font-size: 14px;
  font-weight: 600;
  box-shadow: 0 8px 20px rgba(0,0,0,0.2);
  z-index: 10001;
  opacity: 0;
  transform: translateY(-20px);
  transition: all 0.3s ease;
}

.fc-notification.show {
  opacity: 1;
  transform: translateY(0);
}

/* ===== LOADING & NO RESULTS ===== */
.fc-loading, .fc-no-results {
  text-align: center;
  padding: 40px;
  color: #B0A298;
  background: #FEFCF9;
  border-radius: 20px;
  margin: 20px;
  font-size: 14px;
}

/* ===== SUGGESTIONS SECTION ===== */
.fc-suggestions-title {
  font-size: 16px;
  font-weight: 700;
  color: #2C2420;
  margin: 20px 20px 10px 20px;
}

.fc-suggestion-item {
  background: #FFFFFF;
  padding: 15px 20px;
  margin: 0;
  border-bottom: 1px solid #F0E8E0;
  cursor: pointer;
  display: flex;
  align-items: center;
  gap: 18px;
  transition: background 0.2s ease;
}

.fc-suggestion-item:hover {
  background: #FEF9F5;
}

.fc-suggestion-image {
  width: 50px;
  height: 50px;
  object-fit: contain;
  border-radius: 10px;
  background: #FFFFFF;
  padding: 5px;
  border: 1px solid #EDE5DC;
}

.fc-suggestion-info {
  flex: 1;
}

.fc-suggestion-name {
  font-size: 14px;
  font-weight: 600;
  color: #2C2420;
  margin-bottom: 2px;
}

.fc-suggestion-price {
  font-size: 13px;
  font-weight: 600;
  color: #C41E3A;
}

.fc-suggestion-tag {
  font-size: 11px;
  color: #8A7A70;
  margin-top: 2px;
}

/* ===== RESPONSIVE ===== */
@media (max-width: 520px) {
  .fc-search-popup {
    width: 90vw;
    right: 5vw;
    left: 5vw;
    bottom: 100px;
  }
  
  .fc-offer-banner {
    margin: 15px 15px 15px 15px;
    padding: 15px;
  }
  
  .fc-info-section, .fc-logo-section, .fc-search-section, .fc-steps-section {
    padding-left: 15px;
    padding-right: 15px;
  }
  
  .fc-steps-box {
    padding: 20px 15px;
  }
  
  .fc-offer-text strong {
    font-size: 14px;
  }
  
  .fc-offer-text span {
    font-size: 11px;
  }
  
  .fc-offer-status {
    font-size: 13px;
    padding: 5px 10px;
    min-width: 55px;
  }
}
</style>

<div class="fc-float-wrapper">
  <!-- Floating Button -->
  <div class="fc-search-toggle" id="fcStickyButton" onclick="window.fcToggleSearch()">
    <svg viewBox="0 0 24 24">
      <circle cx="11" cy="11" r="8" stroke="#FFFFFF" fill="none"/>
      <line x1="21" y1="21" x2="16.65" y2="16.65" stroke="#FFFFFF" stroke-width="2"/>
    </svg>
    <span class="fc-toggle-text">Suche</span>
  </div>

  <!-- Search Popup - PREMIUM DESIGN -->
  <div class="fc-search-popup" id="fcSearchPopup">
    <div class="fc-close-btn" onclick="window.fcToggleSearch()">✕</div>

    <!-- OFFER BANNER - 3 KAUFEN 1 GRATIS mit verbesserter UI -->
    <div class="fc-offer-banner" id="fcOfferBanner">
      <div class="fc-offer-left">
        <span class="fc-offer-icon">🎁</span>
        <div class="fc-offer-text">
          <strong>3 KAUFEN 1 GRATIS</strong>
          <span id="fcOfferSubtext">Füge 3 Artikel hinzu für ein gratis Geschenk</span>
        </div>
      </div>
      <div class="fc-offer-status" id="fcOfferStatus">0/3</div>
    </div>
    
    <!-- Progress bar - verbesserter Abstand -->
    <div class="fc-offer-progress-container">
      <div class="fc-offer-progress-bar">
        <div class="fc-offer-progress-fill" id="fcOfferProgress" style="width: 0%;"></div>
      </div>
      <div class="fc-offer-message" id="fcOfferMessage">Füge 3 Artikel hinzu für ein gratis Geschenk</div>
    </div>

    <!-- INFORMATION PROVIDED BY: Section -->
    <div class="fc-info-section">
      <div class="fc-info-label">INFORMATIONEN BEREITGESTELLT VON:</div>
    </div>

    <!-- Logo Section - CLICKABLE linking to fragrancecode.com -->
    <a href="https://fragrancecode.com" target="_blank" style="text-decoration: none;">
      <div class="fc-logo-section">
        <div class="fc-logo-red">
          <span class="fc-logo-fc">FC</span>
        </div>
        <span class="fc-brand-text">FRAGRANCE CODE</span>
      </div>
    </a>

    <!-- Search Input with Clear Button -->
    <div class="fc-search-section">
      <div class="fc-search-container">
        <input type="text" 
               id="fcSearchInput" 
               class="fc-search-field" 
               placeholder="Suche nach Name, Marke, ID oder Duftnoten..."
               autocomplete="off">
        <button class="fc-clear-search" id="fcClearSearch">✕</button>
      </div>
    </div>

    <!-- Steps Section - PREMIUM BOX with bold text -->
    <div class="fc-steps-section" id="fcSteps">
      <div class="fc-steps-box">
        <div class="fc-steps-title">
          SO FINDEST DU<br><span>DEIN PARFUM.</span>
        </div>
        <div class="fc-step-item">
          <span class="fc-step-number">1</span>
          <span>SAG MIR, WELCHES PARFUM DU LIEBST.</span>
        </div>
        <div class="fc-step-item">
          <span class="fc-step-number">2</span>
          <span>ICH SERVIERE ES DIR IN SEKUNDEN...</span>
        </div>
        <div class="fc-step-item">
          <span class="fc-step-number">3</span>
          <span>ENTDECKE DEIN NEUES LIEBLINGSPARFUM UND GENIESSE ES!</span>
        </div>
      </div>
    </div>

    <!-- Dynamic Content - Search Results -->
    <div id="fcContent"></div>
  </div>
</div>

<script>
(function() {
  // ===== YOUR SHOPIFY PRODUCTS =====
  let fcProducts = [];
  let currentView = 'search';
  let currentSearchQuery = '';
  
  // Debounce timer
  let searchTimer = null;

  // Get elements
  const stickyButton = document.getElementById('fcStickyButton');
  const searchPopup = document.getElementById('fcSearchPopup');
  const searchInput = document.getElementById('fcSearchInput');
  const clearSearch = document.getElementById('fcClearSearch');
  const steps = document.getElementById('fcSteps');
  const content = document.getElementById('fcContent');
  
  // Offer elements
  const offerBanner = document.getElementById('fcOfferBanner');
  const offerStatus = document.getElementById('fcOfferStatus');
  const offerProgress = document.getElementById('fcOfferProgress');
  const offerMessage = document.getElementById('fcOfferMessage');
  const offerSubtext = document.getElementById('fcOfferSubtext');

  // ===== COMBINE ALL PERFUME CATALOGS =====
  let allPerfumes = [];

  function combineCatalogs() {
    allPerfumes = [];
    
    // Add from PerfumeCatalog if it exists
    if (window.PerfumeCatalog) {
      // Get all array properties (brands)
      for (let brandKey in window.PerfumeCatalog) {
        if (Array.isArray(window.PerfumeCatalog[brandKey])) {
          allPerfumes.push(...window.PerfumeCatalog[brandKey]);
        }
      }
    }
    
    console.log(`✓ Kombinierter Katalog: ${allPerfumes.length} Parfums`);
  }

  // ===== EXTRACT PRODUCT NUMBER FROM TITLE =====
  function extractProductNumber(title) {
    if (!title) return null;
    const match = title.match(/No\.?\s*(\d+)/i);
    return match ? match[1] : null;
  }

  // ===== FIND PERFUME BY ID =====
  function findPerfumeById(id) {
    return allPerfumes.find(p => p.id === id);
  }

  // ===== ADVANCED LEVENSHTEIN DISTANCE FOR FUZZY MATCHING =====
  function levenshteinDistance(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    
    const matrix = [];
    
    // Initialize matrix
    for (let i = 0; i <= b.length; i++) {
      matrix[i] = [i];
    }
    
    for (let j = 0; j <= a.length; j++) {
      matrix[0][j] = j;
    }
    
    // Fill matrix
    for (let i = 1; i <= b.length; i++) {
      for (let j = 1; j <= a.length; j++) {
        if (b.charAt(i - 1) === a.charAt(j - 1)) {
          matrix[i][j] = matrix[i - 1][j - 1];
        } else {
          matrix[i][j] = Math.min(
            matrix[i - 1][j - 1] + 1, // substitution
            matrix[i][j - 1] + 1,     // insertion
            matrix[i - 1][j] + 1      // deletion
          );
        }
      }
    }
    
    return matrix[b.length][a.length];
  }

  // ===== NORMALIZE STRING FOR BETTER COMPARISON =====
  function normalizeString(str) {
    if (!str) return '';
    return str.toLowerCase()
              .replace(/[^\w\s]/g, '') // Remove punctuation
              .replace(/\s+/g, ' ')     // Normalize spaces
              .trim();
  }

  // ===== GENERATE QUERY VARIATIONS FOR BETTER MATCHING =====
  function generateQueryVariations(query) {
    const variations = [query];
    const queryLower = query.toLowerCase();
    
    // Common brand name misspellings and variations
    const brandMappings = {
      'armani': ['armani', 'amani', 'arani', 'armany', 'armoni', 'armani'],
      'chanel': ['chanel', 'channel', 'chanell', 'shanell', 'chanel'],
      'dior': ['dior', 'diorr', 'diore', 'dior'],
      'sauvage': ['sauvage', 'savages', 'sauvage', 'savage'],
      'bleu': ['bleu', 'blue', 'blu', 'bleue'],
      'noir': ['noir', 'noire', 'nior', 'noir'],
      'musc': ['musc', 'musk', 'musck', 'musc'],
      'million': ['million', 'milion', 'mil lion', '1 million', 'one million'],
      'lucky': ['lucky', 'luky', 'luckey']
    };
    
    // Check each brand mapping
    for (const [correct, misspellings] of Object.entries(brandMappings)) {
      for (const misspelling of misspellings) {
        if (queryLower.includes(misspelling) && misspelling !== correct) {
          variations.push(queryLower.replace(misspelling, correct));
        }
      }
    }
    
    // Handle "chanel blue" vs "bleu de chanel"
    if (queryLower.includes('chanel') && (queryLower.includes('blue') || queryLower.includes('bleu'))) {
      variations.push('bleu de chanel');
      variations.push('bleu chanel');
    }
    
    if (queryLower.includes('bleu') && queryLower.includes('chanel')) {
      variations.push('chanel blue');
      variations.push('blue chanel');
    }
    
    // Remove numbers from query for broader matching
    const withoutNumbers = query.replace(/\d+/g, '').trim();
    if (withoutNumbers && withoutNumbers !== query) {
      variations.push(withoutNumbers);
    }
    
    // Remove spaces for matching concatenated words
    const withoutSpaces = query.replace(/\s+/g, '');
    if (withoutSpaces !== query) {
      variations.push(withoutSpaces);
    }
    
    return [...new Set(variations)]; // Remove duplicates
  }

  // ===== HARDCODED PRODUCT DATABASE - FALLBACK WENN SHOPIFY PRODUKTE NICHT GEFUNDEN WERDEN =====
  const hardcodedProducts = [
    // PACO RABANNE - One Million Collection
    {
      id: "hardcoded-one-million-275",
      title: "Riecht wie… One Million - No. 275",
      image: "https://magicperfume.co/cdn/shop/files/Paco_Rabanne_One_Million.png?v=1770217929",
      url: "https://magicperfume.co/products/magic-perfume-no-275m",
      variant: 47306572353794,
      price: 1199,
      brand: "Paco Rabanne",
      number: "275",
      searchTerms: ["one million", "1 million", "million", "paco rabanne one million"]
    },
    {
      id: "hardcoded-one-million-lucky-280",
      title: "Riecht wie... One Million Lucky - No. 280",
      image: "https://magicperfume.co/cdn/shop/files/One_Million_Lucky.webp?v=1772135132",
      url: "https://magicperfume.co/products/magic-perfume-no-280m",
      variant: 47306572386562,
      price: 1199,
      brand: "Paco Rabanne",
      number: "280",
      searchTerms: ["one million lucky", "million lucky", "lucky", "paco rabanne lucky"]
    },
    {
      id: "hardcoded-one-million-elixir-334",
      title: "Riecht wie... One Million Elixir - No. 334",
      image: "https://magicperfume.co/cdn/shop/files/Paco_Rabanne_One_Million_Elixir.png?v=1770579856",
      url: "https://magicperfume.co/products/magic-perfume-no-334m",
      variant: 47306572419330,
      price: 1199,
      brand: "Paco Rabanne",
      number: "334",
      searchTerms: ["one million elixir", "million elixir", "elixir"]
    },
    {
      id: "hardcoded-lady-million-023",
      title: "Riecht wie... LADY MILION - No. 023",
      image: "https://magicperfume.co/cdn/shop/files/Paco_Rabanne_Lady_Million.png?v=1770218839",
      url: "https://magicperfume.co/products/magic-perfume-no-023w",
      variant: 47306572452098,
      price: 1199,
      brand: "Paco Rabanne",
      number: "023",
      searchTerms: ["lady million", "lady milion", "lady"]
    },
    {
      id: "hardcoded-fame-498",
      title: "Riecht wie... Fame - No. 498",
      image: "https://magicperfume.co/cdn/shop/files/Paco_Rabanne_Fame.png?v=1770285610",
      url: "https://magicperfume.co/products/magic-perfume-no-498w",
      variant: 47306572484866,
      price: 1199,
      brand: "Paco Rabanne",
      number: "498",
      searchTerms: ["fame", "paco rabanne fame"]
    },
    {
      id: "hardcoded-invictus-228",
      title: "Riecht wie... Invictus - No. 228",
      image: "https://magicperfume.co/cdn/shop/files/Paco_Rabanne_Invictus.png?v=1770286917",
      url: "https://magicperfume.co/products/magic-perfume-no-228m",
      variant: 47306572517634,
      price: 1199,
      brand: "Paco Rabanne",
      number: "228",
      searchTerms: ["invictus", "paco rabanne invictus"]
    },
    {
      id: "hardcoded-invictus-victory-303",
      title: "Riecht wie... Invictus Victory - No. 303",
      image: "https://magicperfume.co/cdn/shop/files/Invictus_Victory.webp?v=1772136784",
      url: "https://magicperfume.co/products/magic-perfume-no-303m",
      variant: 47306572550402,
      price: 1199,
      brand: "Paco Rabanne",
      number: "303",
      searchTerms: ["invictus victory", "victory"]
    },
    {
      id: "hardcoded-phantom-399",
      title: "Riecht wie... Phantom - No. 399",
      image: "https://magicperfume.co/cdn/shop/files/Paco_Rabanne_Phantom.png?v=1770580078",
      url: "https://magicperfume.co/products/magic-perfume-no-399m",
      variant: 47306572583170,
      price: 1199,
      brand: "Paco Rabanne",
      number: "399",
      searchTerms: ["phantom", "paco rabanne phantom"]
    },
    {
      id: "hardcoded-pure-xs-for-her-050",
      title: "Riecht wie... Pure XS For Her - No. 050",
      image: "https://magicperfume.co/cdn/shop/files/Paco_Rabanne_XS_For_Her.jpg?v=1772105675",
      url: "https://magicperfume.co/products/magic-perfume-no-050w",
      variant: 47306572615938,
      price: 1199,
      brand: "Paco Rabanne",
      number: "050",
      searchTerms: ["pure xs", "xs", "pure"]
    },
    {
      id: "hardcoded-olympea-intense-095",
      title: "Riecht wie... Olympea Intense - No. 095",
      image: "https://magicperfume.co/cdn/shop/files/Paco_Rabanne_Olympea_INTENSE.jpg?v=1772108016",
      url: "https://magicperfume.co/products/magic-perfume-no-095w",
      variant: 47306572648706,
      price: 1199,
      brand: "Paco Rabanne",
      number: "095",
      searchTerms: ["olympea", "olympea intense", "intense"]
    },
    {
      id: "hardcoded-ultraviolet-480",
      title: "Riecht wie... Ultraviolet - No. 480",
      image: "https://magicperfume.co/cdn/shop/files/Ultraviolet.webp?v=1772141928",
      url: "https://magicperfume.co/products/magic-perfume-no-480w",
      variant: 47306572681474,
      price: 1199,
      brand: "Paco Rabanne",
      number: "480",
      searchTerms: ["ultraviolet", "ultra violet"]
    },
    
    // ARMANI
    {
      id: "hardcoded-armani-code-260",
      title: "Riecht wie... Armani Code - No. 260",
      image: "https://magicperfume.co/cdn/shop/files/Giorgio_Armani_Armani_Code.png?v=1770220395",
      url: "https://magicperfume.co/products/magic-perfume-no-260m",
      variant: 47306572714242,
      price: 1199,
      brand: "Giorgio Armani",
      number: "260",
      searchTerms: ["armani code", "code", "armani"]
    },
    {
      id: "hardcoded-code-for-woman-135",
      title: "Riecht wie... Code for Woman - No. 135",
      image: "https://magicperfume.co/cdn/shop/files/Giorgio_Armani_Code_for_Woman.jpg?v=1771252264",
      url: "https://magicperfume.co/products/magic-perfume-no-135w",
      variant: 47306572747010,
      price: 1199,
      brand: "Giorgio Armani",
      number: "135",
      searchTerms: ["code for woman", "armani code woman", "code woman"]
    },
    
    // CHANEL
    {
      id: "hardcoded-bleu-252",
      title: "Riecht wie... Bleu - No. 252",
      image: "https://magicperfume.co/cdn/shop/files/Chanel_Bleud.png?v=1770283838",
      url: "https://magicperfume.co/products/magic-perfume-no-252m",
      variant: 47306572779778,
      price: 1199,
      brand: "Chanel",
      number: "252",
      searchTerms: ["bleu", "bleu de chanel", "chanel bleu", "blue"]
    },
    {
      id: "hardcoded-no5-077",
      title: "Riecht wie... No.5 - No. 077",
      image: "https://magicperfume.co/cdn/shop/files/Chanel.jpg?v=1771891018",
      url: "https://magicperfume.co/products/magic-perfume-no-077w",
      variant: 47306572812546,
      price: 1199,
      brand: "Chanel",
      number: "077",
      searchTerms: ["no 5", "no5", "chanel no5", "number 5"]
    },
    
    // NARCISO RODRIGUEZ
    {
      id: "hardcoded-musc-noir-457",
      title: "Riecht wie... Musc Noir For Her - No. 457",
      image: "https://magicperfume.co/cdn/shop/files/Narciso_Rodriguez_Musc_Noir_For_Her.png?v=1770292273",
      url: "https://magicperfume.co/products/magic-perfume-no-457w",
      variant: 47306572845314,
      price: 1199,
      brand: "Narciso Rodriguez",
      number: "457",
      searchTerms: ["musc noir", "narciso", "noir"]
    }
  ];

  // ===== SEARCH IN HARDCODED PRODUCTS =====
  function searchHardcodedProducts(query) {
    if (!query || query.length < 1) return [];
    
    const queryLower = query.toLowerCase().trim();
    const results = [];
    const addedIds = new Set();
    
    console.log('🔍 Suche in Hardcoded-Produkten für:', queryLower);
    
    hardcodedProducts.forEach(product => {
      // Check title
      if (product.title.toLowerCase().includes(queryLower)) {
        results.push({ product, score: 100, matchReasons: ['hardcoded-titel'] });
        addedIds.add(product.id);
        return;
      }
      
      // Check search terms
      if (product.searchTerms) {
        for (const term of product.searchTerms) {
          if (term.includes(queryLower) || queryLower.includes(term)) {
            results.push({ product, score: 90, matchReasons: ['hardcoded-suchbegriff'] });
            addedIds.add(product.id);
            return;
          }
        }
      }
      
      // Check number
      if (product.number && queryLower.includes(product.number)) {
        results.push({ product, score: 95, matchReasons: ['hardcoded-nummer'] });
        addedIds.add(product.id);
        return;
      }
      
      // Check brand
      if (product.brand.toLowerCase().includes(queryLower)) {
        results.push({ product, score: 85, matchReasons: ['hardcoded-marke'] });
        addedIds.add(product.id);
        return;
      }
    });
    
    return results;
  }

  // ===== FIXED CATALOG SEARCH - HARDCODED MATCHING ONLY =====
  function advancedCatalogSearch(query) {
    if (!query || query.length < 1 || !allPerfumes || allPerfumes.length === 0) {
      return [];
    }
    
    const queryLower = query.toLowerCase().trim();
    const results = [];
    const addedIds = new Set();
    
    console.log('🔍 Katalogsuche für:', queryLower);
    
    // ===== STEP 1: CHECK FOR EXACT PRODUCT NUMBER =====
    const numberMatches = queryLower.match(/\d+/g);
    if (numberMatches) {
      for (const num of numberMatches) {
        const exactMatch = allPerfumes.find(p => 
          p.productNumbers && p.productNumbers.includes(num)
        );
        
        if (exactMatch && !addedIds.has(exactMatch.id)) {
          results.push(exactMatch);
          addedIds.add(exactMatch.id);
          console.log(`✅ Katalog-Treffer über Nummer ${num}:`, exactMatch.name);
        }
      }
    }
    
    // ===== STEP 2: HARDCODED BRAND MAPPINGS =====
    
    // DIOR Products
    if (queryLower.includes('miss dior')) {
      const missDior = allPerfumes.find(p => p.id === 'dior_miss_dior_le_parfum');
      if (missDior && !addedIds.has(missDior.id)) {
        results.push(missDior);
        addedIds.add(missDior.id);
        console.log('✅ Miss Dior Le Parfum gefunden');
      }
    }
    
    if (queryLower.includes('sauvage')) {
      const sauvageParfum = allPerfumes.find(p => p.id === 'dior_sauvage_parfum');
      const sauvageElixir = allPerfumes.find(p => p.id === 'dior_sauvage_elixir');
      
      if (sauvageParfum && !addedIds.has(sauvageParfum.id)) {
        results.push(sauvageParfum);
        addedIds.add(sauvageParfum.id);
      }
      if (sauvageElixir && !addedIds.has(sauvageElixir.id)) {
        results.push(sauvageElixir);
        addedIds.add(sauvageElixir.id);
      }
    }
    
    if (queryLower.includes('jadore') || queryLower.includes('j\'adore')) {
      const jadore = allPerfumes.find(p => p.id === 'dior_jadore');
      if (jadore && !addedIds.has(jadore.id)) {
        results.push(jadore);
        addedIds.add(jadore.id);
      }
    }
    
    if (queryLower.includes('hypnotic poison')) {
      const hypnotic = allPerfumes.find(p => p.id === 'dior_hypnotic_poison');
      if (hypnotic && !addedIds.has(hypnotic.id)) {
        results.push(hypnotic);
        addedIds.add(hypnotic.id);
      }
    }
    
    if (queryLower.includes('fahrenheit')) {
      const fahrenheit = allPerfumes.find(p => p.id === 'dior_fahrenheit');
      const fahrenheitLeParfum = allPerfumes.find(p => p.id === 'dior_fahrenheit_le_parfum');
      const fahrenheit32 = allPerfumes.find(p => p.id === 'dior_fahrenheit_32');
      
      if (fahrenheit && !addedIds.has(fahrenheit.id)) results.push(fahrenheit);
      if (fahrenheitLeParfum && !addedIds.has(fahrenheitLeParfum.id)) results.push(fahrenheitLeParfum);
      if (fahrenheit32 && !addedIds.has(fahrenheit32.id)) results.push(fahrenheit32);
    }
    
    if (queryLower.includes('homme intense')) {
      const hommeIntense = allPerfumes.find(p => p.id === 'dior_homme_intense');
      if (hommeIntense && !addedIds.has(hommeIntense.id)) {
        results.push(hommeIntense);
        addedIds.add(hommeIntense.id);
      }
    }
    
    if (queryLower.includes('miss cherie')) {
      const missCherie = allPerfumes.find(p => p.id === 'dior_miss_cherie');
      if (missCherie && !addedIds.has(missCherie.id)) {
        results.push(missCherie);
        addedIds.add(missCherie.id);
      }
    }
    
    // CHANEL Products
    if (queryLower.includes('bleu') || queryLower.includes('blue')) {
      const bleu = allPerfumes.find(p => p.id === 'chanel_bleu');
      if (bleu && !addedIds.has(bleu.id)) {
        results.push(bleu);
        addedIds.add(bleu.id);
      }
    }
    
    if (queryLower.includes('coco') && !queryLower.includes('mademoiselle')) {
      const coco = allPerfumes.find(p => p.id === 'chanel_coco');
      if (coco && !addedIds.has(coco.id)) {
        results.push(coco);
        addedIds.add(coco.id);
      }
    }
    
    if (queryLower.includes('mademoiselle')) {
      const mademoiselle = allPerfumes.find(p => p.id === 'chanel_mademoiselle');
      const mademoiselleIntense = allPerfumes.find(p => p.id === 'chanel_coco_mademoiselle_intense');
      
      if (mademoiselle && !addedIds.has(mademoiselle.id)) results.push(mademoiselle);
      if (mademoiselleIntense && !addedIds.has(mademoiselleIntense.id)) results.push(mademoiselleIntense);
    }
    
    if (queryLower.includes('chance')) {
      const chance = allPerfumes.find(p => p.id === 'chanel_chance');
      if (chance && !addedIds.has(chance.id)) {
        results.push(chance);
        addedIds.add(chance.id);
      }
    }
    
    if (queryLower.includes('no. 5') || queryLower.includes('no 5') || queryLower.includes('chanel no5')) {
      const no5 = allPerfumes.find(p => p.id === 'chanel_no5');
      if (no5 && !addedIds.has(no5.id)) {
        results.push(no5);
        addedIds.add(no5.id);
      }
    }
    
    if (queryLower.includes('allure homme')) {
      const allure = allPerfumes.find(p => p.id === 'chanel_allure_homme');
      if (allure && !addedIds.has(allure.id)) {
        results.push(allure);
        addedIds.add(allure.id);
      }
    }
    
    // ARMANI Products
    if (queryLower.includes('si')) {
      const si = allPerfumes.find(p => p.id === 'armani_si');
      const siPassione = allPerfumes.find(p => p.id === 'armani_si_passione');
      
      if (si && !addedIds.has(si.id)) results.push(si);
      if (siPassione && !addedIds.has(siPassione.id)) results.push(siPassione);
    }
    
    if (queryLower.includes('emporio she')) {
      const emporioShe = allPerfumes.find(p => p.id === 'armani_emporio_she');
      if (emporioShe && !addedIds.has(emporioShe.id)) {
        results.push(emporioShe);
        addedIds.add(emporioShe.id);
      }
    }
    
    if (queryLower.includes('my way')) {
      const myWay = allPerfumes.find(p => p.id === 'armani_my_way');
      if (myWay && !addedIds.has(myWay.id)) {
        results.push(myWay);
        addedIds.add(myWay.id);
      }
    }
    
    if (queryLower.includes('code') && !queryLower.includes('woman')) {
      const code = allPerfumes.find(p => p.id === 'armani_code');
      if (code && !addedIds.has(code.id)) {
        results.push(code);
        addedIds.add(code.id);
      }
    }
    
    if (queryLower.includes('code for woman') || (queryLower.includes('code') && queryLower.includes('woman'))) {
      const codeWoman = allPerfumes.find(p => p.id === 'armani_code_for_woman');
      if (codeWoman && !addedIds.has(codeWoman.id)) {
        results.push(codeWoman);
        addedIds.add(codeWoman.id);
      }
    }
    
    if (queryLower.includes('acqua di gio')) {
      const acqua = allPerfumes.find(p => p.id === 'armani_acqua_di_gio');
      const acquaGioia = allPerfumes.find(p => p.id === 'armani_acqua_di_gioia');
      
      if (acqua && !addedIds.has(acqua.id)) results.push(acqua);
      if (acquaGioia && !addedIds.has(acquaGioia.id)) results.push(acquaGioia);
    }
    
    if (queryLower.includes('stronger with you')) {
      const swy = allPerfumes.find(p => p.id === 'armani_stronger_with_you_intensely');
      if (swy && !addedIds.has(swy.id)) {
        results.push(swy);
        addedIds.add(swy.id);
      }
    }
    
    if (queryLower.includes('armani diamonds')) {
      const diamonds = allPerfumes.find(p => p.id === 'armani_diamonds');
      if (diamonds && !addedIds.has(diamonds.id)) {
        results.push(diamonds);
        addedIds.add(diamonds.id);
      }
    }
    
    if (queryLower.includes('code sport')) {
      const sport = allPerfumes.find(p => p.id === 'armani_code_sport');
      if (sport && !addedIds.has(sport.id)) {
        results.push(sport);
        addedIds.add(sport.id);
      }
    }
    
    if (queryLower.includes('terra di gioia')) {
      const terra = allPerfumes.find(p => p.id === 'armani_terra_di_gioia');
      if (terra && !addedIds.has(terra.id)) {
        results.push(terra);
        addedIds.add(terra.id);
      }
    }
    
    // PACO RABANNE Products
    if (queryLower.includes('one million lucky')) {
      const lucky = allPerfumes.find(p => p.id === 'paco_one_million_lucky');
      if (lucky && !addedIds.has(lucky.id)) {
        results.push(lucky);
        addedIds.add(lucky.id);
      }
    }
    
    if (queryLower.includes('one million') && !queryLower.includes('lucky') && !queryLower.includes('elixir')) {
      const oneMillion = allPerfumes.find(p => p.id === 'paco_one_million');
      if (oneMillion && !addedIds.has(oneMillion.id)) {
        results.push(oneMillion);
        addedIds.add(oneMillion.id);
      }
    }
    
    if (queryLower.includes('one million elixir')) {
      const elixir = allPerfumes.find(p => p.id === 'paco_one_million_elixir');
      if (elixir && !addedIds.has(elixir.id)) {
        results.push(elixir);
        addedIds.add(elixir.id);
      }
    }
    
    if (queryLower.includes('lady million')) {
      const lady = allPerfumes.find(p => p.id === 'paco_lady_million');
      if (lady && !addedIds.has(lady.id)) {
        results.push(lady);
        addedIds.add(lady.id);
      }
    }
    
    if (queryLower.includes('fame')) {
      const fame = allPerfumes.find(p => p.id === 'paco_fame');
      if (fame && !addedIds.has(fame.id)) {
        results.push(fame);
        addedIds.add(fame.id);
      }
    }
    
    if (queryLower.includes('invictus') && !queryLower.includes('victory')) {
      const invictus = allPerfumes.find(p => p.id === 'paco_invictus');
      if (invictus && !addedIds.has(invictus.id)) {
        results.push(invictus);
        addedIds.add(invictus.id);
      }
    }
    
    if (queryLower.includes('invictus victory')) {
      const victory = allPerfumes.find(p => p.id === 'paco_invictus_victory');
      if (victory && !addedIds.has(victory.id)) {
        results.push(victory);
        addedIds.add(victory.id);
      }
    }
    
    if (queryLower.includes('phantom')) {
      const phantom = allPerfumes.find(p => p.id === 'paco_phantom');
      if (phantom && !addedIds.has(phantom.id)) {
        results.push(phantom);
        addedIds.add(phantom.id);
      }
    }
    
    if (queryLower.includes('pure xs')) {
      const pureXs = allPerfumes.find(p => p.id === 'paco_pure_xs_for_her');
      if (pureXs && !addedIds.has(pureXs.id)) {
        results.push(pureXs);
        addedIds.add(pureXs.id);
      }
    }
    
    if (queryLower.includes('olympea')) {
      const olympea = allPerfumes.find(p => p.id === 'paco_olympea_intense');
      if (olympea && !addedIds.has(olympea.id)) {
        results.push(olympea);
        addedIds.add(olympea.id);
      }
    }
    
    if (queryLower.includes('ultraviolet')) {
      const ultra = allPerfumes.find(p => p.id === 'paco_ultraviolet');
      if (ultra && !addedIds.has(ultra.id)) {
        results.push(ultra);
        addedIds.add(ultra.id);
      }
    }
    
    // YSL Products
    if (queryLower.includes('libre')) {
      const libre = allPerfumes.find(p => p.id === 'ysl_libre');
      if (libre && !addedIds.has(libre.id)) {
        results.push(libre);
        addedIds.add(libre.id);
      }
    }
    
    if (queryLower.includes('black opium')) {
      const blackOpium = allPerfumes.find(p => p.id === 'ysl_black_opium');
      if (blackOpium && !addedIds.has(blackOpium.id)) {
        results.push(blackOpium);
        addedIds.add(blackOpium.id);
      }
    }
    
    if (queryLower.includes('opium') && !queryLower.includes('black')) {
      const opium = allPerfumes.find(p => p.id === 'ysl_opium');
      if (opium && !addedIds.has(opium.id)) {
        results.push(opium);
        addedIds.add(opium.id);
      }
    }
    
    if (queryLower.includes('y') && queryLower.length < 3) {
      const y = allPerfumes.find(p => p.id === 'ysl_y');
      if (y && !addedIds.has(y.id)) {
        results.push(y);
        addedIds.add(y.id);
      }
    }
    
    // VALENTINO
    if (queryLower.includes('born in roma')) {
      const born = allPerfumes.find(p => p.id === 'valentino_born_in_roma');
      const uomo = allPerfumes.find(p => p.id === 'valentino_uomo_born_in_roma');
      
      if (born && !addedIds.has(born.id)) results.push(born);
      if (uomo && !addedIds.has(uomo.id)) results.push(uomo);
    }
    
    // PRADA
    if (queryLower.includes('paradoxe')) {
      const paradoxe = allPerfumes.find(p => p.id === 'prada_paradoxe');
      if (paradoxe && !addedIds.has(paradoxe.id)) {
        results.push(paradoxe);
        addedIds.add(paradoxe.id);
      }
    }
    
    if (queryLower.includes('candy')) {
      const candy = allPerfumes.find(p => p.id === 'prada_candy');
      if (candy && !addedIds.has(candy.id)) {
        results.push(candy);
        addedIds.add(candy.id);
      }
    }
    
    // LANCOME
    if (queryLower.includes('la vie est belle')) {
      const lveb = allPerfumes.find(p => p.id === 'lancome_la_vie_est_belle');
      const intensement = allPerfumes.find(p => p.id === 'lancome_la_vie_est_belle_intensement');
      
      if (lveb && !addedIds.has(lveb.id)) results.push(lveb);
      if (intensement && !addedIds.has(intensement.id)) results.push(intensement);
    }
    
    if (queryLower.includes('tresor')) {
      const tresor = allPerfumes.find(p => p.id === 'lancome_tresor');
      if (tresor && !addedIds.has(tresor.id)) {
        results.push(tresor);
        addedIds.add(tresor.id);
      }
    }
    
    if (queryLower.includes('idole')) {
      const idole = allPerfumes.find(p => p.id === 'lancome_idole');
      if (idole && !addedIds.has(idole.id)) {
        results.push(idole);
        addedIds.add(idole.id);
      }
    }
    
    // CAROLINA HERRERA
    if (queryLower.includes('good girl')) {
      const goodGirl = allPerfumes.find(p => p.id === 'carolina_good_girl');
      const veryGoodGirl = allPerfumes.find(p => p.id === 'carolina_very_good_girl');
      
      if (goodGirl && !addedIds.has(goodGirl.id)) results.push(goodGirl);
      if (veryGoodGirl && !addedIds.has(veryGoodGirl.id)) results.push(veryGoodGirl);
    }
    
    // JEAN PAUL GAULTIER
    if (queryLower.includes('scandal')) {
      const scandal = allPerfumes.find(p => p.id === 'jpg_scandal');
      if (scandal && !addedIds.has(scandal.id)) {
        results.push(scandal);
        addedIds.add(scandal.id);
      }
    }
    
    if (queryLower.includes('la belle')) {
      const laBelle = allPerfumes.find(p => p.id === 'jpg_la_belle');
      if (laBelle && !addedIds.has(laBelle.id)) {
        results.push(laBelle);
        addedIds.add(laBelle.id);
      }
    }
    
    if (queryLower.includes('ultra male')) {
      const ultraMale = allPerfumes.find(p => p.id === 'jpg_ultra_male');
      if (ultraMale && !addedIds.has(ultraMale.id)) {
        results.push(ultraMale);
        addedIds.add(ultraMale.id);
      }
    }
    
    if (queryLower.includes('classique')) {
      const classique = allPerfumes.find(p => p.id === 'jpg_classique_essence');
      if (classique && !addedIds.has(classique.id)) {
        results.push(classique);
        addedIds.add(classique.id);
      }
    }
    
    if (queryLower.includes('le male')) {
      const leMale = allPerfumes.find(p => p.id === 'jpg_le_male');
      if (leMale && !addedIds.has(leMale.id)) {
        results.push(leMale);
        addedIds.add(leMale.id);
      }
    }
    
    // MUGLER
    if (queryLower.includes('angel') && !queryLower.includes('nova')) {
      const angel = allPerfumes.find(p => p.id === 'mugler_angel');
      if (angel && !addedIds.has(angel.id)) {
        results.push(angel);
        addedIds.add(angel.id);
      }
    }
    
    if (queryLower.includes('angel nova')) {
      const nova = allPerfumes.find(p => p.id === 'mugler_angel_nova');
      if (nova && !addedIds.has(nova.id)) {
        results.push(nova);
        addedIds.add(nova.id);
      }
    }
    
    if (queryLower.includes('a*men') || queryLower.includes('amen')) {
      const amen = allPerfumes.find(p => p.id === 'mugler_a_men');
      if (amen && !addedIds.has(amen.id)) {
        results.push(amen);
        addedIds.add(amen.id);
      }
    }
    
    // TOM FORD
    if (queryLower.includes('vanille fatale')) {
      const vf = allPerfumes.find(p => p.id === 'tomford_vanille_fatale');
      if (vf && !addedIds.has(vf.id)) {
        results.push(vf);
        addedIds.add(vf.id);
      }
    }
    
    if (queryLower.includes('lost cherry')) {
      const lc = allPerfumes.find(p => p.id === 'tomford_lost_cherry');
      if (lc && !addedIds.has(lc.id)) {
        results.push(lc);
        addedIds.add(lc.id);
      }
    }
    
    if (queryLower.includes('fucking fabulous')) {
      const ff = allPerfumes.find(p => p.id === 'tomford_fucking_fabulous');
      if (ff && !addedIds.has(ff.id)) {
        results.push(ff);
        addedIds.add(ff.id);
      }
    }
    
    if (queryLower.includes('tobacco vanille')) {
      const tv = allPerfumes.find(p => p.id === 'tomford_tobacco_vanille');
      if (tv && !addedIds.has(tv.id)) {
        results.push(tv);
        addedIds.add(tv.id);
      }
    }
    
    if (queryLower.includes('oud wood')) {
      const ow = allPerfumes.find(p => p.id === 'tomford_oud_wood');
      if (ow && !addedIds.has(ow.id)) {
        results.push(ow);
        addedIds.add(ow.id);
      }
    }
    
    // HUGO BOSS
    if (queryLower.includes('boss woman')) {
      const bw = allPerfumes.find(p => p.id === 'boss_woman');
      if (bw && !addedIds.has(bw.id)) {
        results.push(bw);
        addedIds.add(bw.id);
      }
    }
    
    if (queryLower.includes('boss bottled')) {
      const bb = allPerfumes.find(p => p.id === 'boss_bottled');
      const bbi = allPerfumes.find(p => p.id === 'boss_bottled_intense');
      
      if (bb && !addedIds.has(bb.id)) results.push(bb);
      if (bbi && !addedIds.has(bbi.id)) results.push(bbi);
    }
    
    if (queryLower.includes('hugo')) {
      const hugo = allPerfumes.find(p => p.id === 'boss_hugo');
      if (hugo && !addedIds.has(hugo.id)) {
        results.push(hugo);
        addedIds.add(hugo.id);
      }
    }
    
    if (queryLower.includes('boss orange')) {
      const orange = allPerfumes.find(p => p.id === 'boss_orange');
      if (orange && !addedIds.has(orange.id)) {
        results.push(orange);
        addedIds.add(orange.id);
      }
    }
    
    if (queryLower.includes('the scent')) {
      const scent = allPerfumes.find(p => p.id === 'boss_the_scent');
      if (scent && !addedIds.has(scent.id)) {
        results.push(scent);
        addedIds.add(scent.id);
      }
    }
    
    if (queryLower.includes('alive')) {
      const alive = allPerfumes.find(p => p.id === 'boss_alive');
      if (alive && !addedIds.has(alive.id)) {
        results.push(alive);
        addedIds.add(alive.id);
      }
    }
    
    if (queryLower.includes('ma vie')) {
      const maVie = allPerfumes.find(p => p.id === 'boss_ma_vie');
      if (maVie && !addedIds.has(maVie.id)) {
        results.push(maVie);
        addedIds.add(maVie.id);
      }
    }
    
    // CALVIN KLEIN
    if (queryLower.includes('eternity')) {
      const eternity = allPerfumes.find(p => p.id === 'ck_eternity');
      const moment = allPerfumes.find(p => p.id === 'ck_eternity_moment');
      
      if (eternity && !addedIds.has(eternity.id)) results.push(eternity);
      if (moment && !addedIds.has(moment.id)) results.push(moment);
    }
    
    if (queryLower.includes('euphoria')) {
      const euphoria = allPerfumes.find(p => p.id === 'ck_euphoria_blossom');
      if (euphoria && !addedIds.has(euphoria.id)) {
        results.push(euphoria);
        addedIds.add(euphoria.id);
      }
    }
    
    if (queryLower.includes('ck one')) {
      const ckOne = allPerfumes.find(p => p.id === 'ck_one');
      if (ckOne && !addedIds.has(ckOne.id)) {
        results.push(ckOne);
        addedIds.add(ckOne.id);
      }
    }
    
    if (queryLower.includes('glow')) {
      const glow = allPerfumes.find(p => p.id === 'ck_glow');
      if (glow && !addedIds.has(glow.id)) {
        results.push(glow);
        addedIds.add(glow.id);
      }
    }
    
    // NARCISO
    if (queryLower.includes('musc noir')) {
      const muscNoir = allPerfumes.find(p => p.id === 'narciso_musc_noir');
      if (muscNoir && !addedIds.has(muscNoir.id)) {
        results.push(muscNoir);
        addedIds.add(muscNoir.id);
      }
    }
    
    // DOLCE & GABBANA
    if (queryLower.includes('light blue')) {
      const lightBlue = allPerfumes.find(p => p.id === 'dolce_light_blue');
      if (lightBlue && !addedIds.has(lightBlue.id)) {
        results.push(lightBlue);
        addedIds.add(lightBlue.id);
      }
    }
    
    if (queryLower.includes('the one')) {
      const theOne = allPerfumes.find(p => p.id === 'dolce_the_one');
      if (theOne && !addedIds.has(theOne.id)) {
        results.push(theOne);
        addedIds.add(theOne.id);
      }
    }
    
    if (queryLower.includes('dolci')) {
      const dolci = allPerfumes.find(p => p.id === 'dolce_dolci');
      if (dolci && !addedIds.has(dolci.id)) {
        results.push(dolci);
        addedIds.add(dolci.id);
      }
    }
    
    // KENZO
    if (queryLower.includes('flower')) {
      const flower = allPerfumes.find(p => p.id === 'kenzo_flower');
      if (flower && !addedIds.has(flower.id)) {
        results.push(flower);
        addedIds.add(flower.id);
      }
    }
    
    // CHLOE
    if (queryLower.includes('chloe') || queryLower.includes('äquivalent')) {
      const chloe = allPerfumes.find(p => p.id === 'chloe_chloe');
      if (chloe && !addedIds.has(chloe.id)) {
        results.push(chloe);
        addedIds.add(chloe.id);
      }
    }
    
    // HERMES
    if (queryLower.includes('terre')) {
      const terre = allPerfumes.find(p => p.id === 'hermes_terre');
      if (terre && !addedIds.has(terre.id)) {
        results.push(terre);
        addedIds.add(terre.id);
      }
    }
    
    // GUCCI
    if (queryLower.includes('rush')) {
      const rush = allPerfumes.find(p => p.id === 'gucci_rush');
      if (rush && !addedIds.has(rush.id)) {
        results.push(rush);
        addedIds.add(rush.id);
      }
    }
    
    if (queryLower.includes('envy me')) {
      const envy = allPerfumes.find(p => p.id === 'gucci_envy_me');
      if (envy && !addedIds.has(envy.id)) {
        results.push(envy);
        addedIds.add(envy.id);
      }
    }
    
    // FERRAGAMO
    if (queryLower.includes('deep red')) {
      const deepRed = allPerfumes.find(p => p.id === 'ferragamo_deep_red');
      if (deepRed && !addedIds.has(deepRed.id)) {
        results.push(deepRed);
        addedIds.add(deepRed.id);
      }
    }
    
    // DAVIDOFF
    if (queryLower.includes('cool water')) {
      const coolWater = allPerfumes.find(p => p.id === 'davidoff_cool_water');
      if (coolWater && !addedIds.has(coolWater.id)) {
        results.push(coolWater);
        addedIds.add(coolWater.id);
      }
    }
    
    // ADIDAS
    if (queryLower.includes('adventure')) {
      const adventure = allPerfumes.find(p => p.id === 'adidas_adventure');
      if (adventure && !addedIds.has(adventure.id)) {
        results.push(adventure);
        addedIds.add(adventure.id);
      }
    }
    
    // DOLCE VITA
    if (queryLower.includes('vita')) {
      const vita = allPerfumes.find(p => p.id === 'dolce_vita_vita');
      if (vita && !addedIds.has(vita.id)) {
        results.push(vita);
        addedIds.add(vita.id);
      }
    }
    
    // DIESEL
    if (queryLower.includes('only the brave')) {
      const brave = allPerfumes.find(p => p.id === 'diesel_only_the_brave');
      if (brave && !addedIds.has(brave.id)) {
        results.push(brave);
        addedIds.add(brave.id);
      }
    }
    
    // CAMPBELL
    if (queryLower.includes('campbell')) {
      const campbell = allPerfumes.find(p => p.id === 'campbell_campbell');
      if (campbell && !addedIds.has(campbell.id)) {
        results.push(campbell);
        addedIds.add(campbell.id);
      }
    }
    
    // CACHAREL
    if (queryLower.includes('amor amor')) {
      const amor = allPerfumes.find(p => p.id === 'cacharel_amor_amor');
      if (amor && !addedIds.has(amor.id)) {
        results.push(amor);
        addedIds.add(amor.id);
      }
    }
    
    // CARTIER
    if (queryLower.includes('la panthere')) {
      const panthere = allPerfumes.find(p => p.id === 'cartier_la_panthere');
      if (panthere && !addedIds.has(panthere.id)) {
        results.push(panthere);
        addedIds.add(panthere.id);
      }
    }
    
    // JOOP
    if (queryLower.includes('joop')) {
      const joop = allPerfumes.find(p => p.id === 'joop_homme');
      if (joop && !addedIds.has(joop.id)) {
        results.push(joop);
        addedIds.add(joop.id);
      }
    }
    
    console.log(`📊 ${results.length} Katalog-Ergebnisse für "${query}" gefunden`);
    
    return results;
  }

  // ===== ADVANCED PRODUCT SEARCH WITH FUZZY MATCHING =====
  function searchProducts(query) {
    if (!query || query.length < 1 || !fcProducts || fcProducts.length === 0) {
      return [];
    }
    
    const queryLower = query.toLowerCase().trim();
    const results = [];
    const addedIds = new Set();
    
    console.log('🔍 Erweiterte Produktsuche für:', queryLower);
    
    // FIRST: Special handling for "code"
    if (queryLower.includes('code')) {
      // Find No. 260 (men's)
      const no260 = fcProducts.find(p => 
        p.title.toLowerCase().includes('no. 260') || 
        p.title.toLowerCase().includes('no 260')
      );
      
      if (no260) {
        results.push({ product: no260, score: 1000 });
        addedIds.add(no260.id);
        console.log('✅ No. 260 für "code" gefunden');
      }
      
      // Find No. 135 (women's)
      const no135 = fcProducts.find(p => 
        p.title.toLowerCase().includes('no. 135') || 
        p.title.toLowerCase().includes('no 135')
      );
      
      if (no135 && !addedIds.has(no135.id)) {
        results.push({ product: no135, score: 900 });
        addedIds.add(no135.id);
        console.log('✅ No. 135 für "code" gefunden');
      }
    }
    
    // SECOND: Special handling for Paco Rabanne products
    const pacoKeywords = ['million', 'lucky', 'lady', 'fame', 'invictus', 'phantom', 'victory', 'olympea', 'ultraviolet', 'pure xs'];
    for (const keyword of pacoKeywords) {
      if (queryLower.includes(keyword)) {
        const pacoProduct = fcProducts.find(p => 
          p.title.toLowerCase().includes(keyword) || 
          p.title.toLowerCase().includes('paco rabanne')
        );
        
        if (pacoProduct && !addedIds.has(pacoProduct.id)) {
          results.push({ product: pacoProduct, score: 950 });
          addedIds.add(pacoProduct.id);
          console.log(`✅ Paco Rabanne Produkt für "${keyword}" gefunden`);
        }
      }
    }
    
    // THIRD: Special handling for "one million lucky"
    if (queryLower.includes('one million lucky') || 
        queryLower.includes('million lucky') || 
        (queryLower.includes('one million') && queryLower.includes('lucky'))) {
      
      const oneMillionLucky = fcProducts.find(p => 
        p.title.toLowerCase().includes('one million lucky') ||
        p.title.includes('No. 280') ||
        p.title.includes('No.280')
      );
      
      if (oneMillionLucky && !addedIds.has(oneMillionLucky.id)) {
        results.push({ product: oneMillionLucky, score: 1000 });
        addedIds.add(oneMillionLucky.id);
        console.log('✅ One Million Lucky DIREKT gefunden');
      }
    }
    
    // Create variations of the search query for better matching
    const queryVariations = generateQueryVariations(queryLower);
    console.log('Produkt-Suchvariationen:', queryVariations);
    
    // Check ALL products with advanced fuzzy matching
    fcProducts.forEach(product => {
      if (addedIds.has(product.id)) return;
      
      const title = product.title;
      const titleLower = title.toLowerCase();
      let bestScore = 0;
      let matchReasons = [];
      
      // Check each variation against the title
      for (const variation of queryVariations) {
        // Direct contains match (highest score)
        if (titleLower.includes(variation)) {
          bestScore = Math.max(bestScore, 100);
          matchReasons.push(`direkt:${variation}`);
        }
      }
      
      // If no direct match, try advanced similarity
      if (bestScore < 50) {
        const similarityScore = calculateAdvancedSimilarity(queryLower, titleLower);
        if (similarityScore > 40) { // 40% similarity threshold
          bestScore = Math.max(bestScore, similarityScore);
          matchReasons.push(`unscharf:${Math.round(similarityScore)}%`);
        }
      }
      
      // Check for number matches (always exact)
      const numberMatch = product.title.match(/No\.?\s*(\d+)/i);
      if (numberMatch && queryLower.includes(numberMatch[1])) {
        bestScore = Math.max(bestScore, 150);
        matchReasons.push(`nummer:${numberMatch[1]}`);
      }
      
      // Check brand name in metafields
      const nuta = product.metafields?.custom?.nuta?.toLowerCase() || '';
      if (nuta) {
        const brandScore = calculateAdvancedSimilarity(queryLower, nuta);
        if (brandScore > 50) {
          bestScore = Math.max(bestScore, brandScore * 0.8);
          matchReasons.push(`marke:${Math.round(brandScore)}%`);
        }
      }
      
      if (bestScore > 30) {
        results.push({ 
          product, 
          score: Math.round(bestScore),
          matchReasons 
        });
        addedIds.add(product.id);
        console.log(`✅ Produkt-Treffer: ${product.title} (Punktzahl: ${Math.round(bestScore)})`, matchReasons);
      }
    });
    
    // Sort by score (highest first)
    results.sort((a, b) => b.score - a.score);
    
    console.log(`📊 ${results.length} Produkt-Ergebnisse für "${query}" gefunden`);
    if (results.length > 0) {
      console.log('🏆 Top-Produkt:', results[0].product.title, 'Punktzahl:', results[0].score);
    }
    
    return results;
  }

  // ===== GET POPULAR PRODUCTS FOR SUGGESTIONS =====
  function getPopularSuggestions() {
    if (!fcProducts || fcProducts.length === 0) return [];
    
    // Return top products as suggestions
    return fcProducts.slice(0, 8);
  }

  // ===== RENDER SUGGESTIONS =====
  function renderSuggestions() {
    const suggestions = getPopularSuggestions();
    
    if (suggestions.length === 0) return '';
    
    let html = `<div class="fc-suggestions-title">✨ Beliebte Düfte</div>`;
    
    suggestions.forEach(product => {
      const priceFormatted = product.price ? '€' + (product.price / 100).toFixed(2) : '€11.99';
      
      html += `
        <div class="fc-suggestion-item" onclick="window.fcViewDetails('${product.url}')">
          <img class="fc-suggestion-image" src="${product.image}" alt="${product.title}" loading="lazy">
          <div class="fc-suggestion-info">
            <div class="fc-suggestion-name">${product.title}</div>
            <div class="fc-suggestion-price">${priceFormatted}</div>
          </div>
        </div>
      `;
    });
    
    return html;
  }

  // ===== RENDER CATALOG PERFUME LIST =====
  function renderCatalogPerfumes(perfumes, query) {
    if (!perfumes || perfumes.length === 0) return '';
    
    const sortedPerfumes = [...perfumes].sort((a, b) => a.name.localeCompare(b.name));
    
    let html = `<div class="fc-section-title">Echte Parfums (${perfumes.length})</div>`;
    
    sortedPerfumes.slice(0, 8).forEach(perfume => {
      html += `
        <div class="fc-real-perfume-item" onclick="window.fcShowPerfumeDetail('${perfume.id}')">
          <img class="fc-real-perfume-image" src="${perfume.image}" 
               alt="${perfume.name}" 
               loading="lazy"
               onerror="this.src='https://via.placeholder.com/60x60?text=Parfum'">
          <div class="fc-real-perfume-info">
            <div class="fc-real-perfume-name">${perfume.name}</div>
            <div class="fc-real-perfume-brand">${perfume.brand}</div>
            ${perfume.year ? `<div class="fc-real-perfume-year">${perfume.year}</div>` : ''}
          </div>
        </div>
      `;
    });
    
    return html;
  }

  // ===== SHOW PERFUME DETAIL WITH HARDCODED MAPPINGS =====
  window.fcShowPerfumeDetail = function(perfumeId) {
    const perfume = findPerfumeById(perfumeId);
    if (!perfume) return;
    
    currentView = 'detail';
    steps.style.display = 'none';
    
    // Find matching Shopify product using hardcoded mappings
    const shopifyProduct = findShopifyProductByCatalogPerfume(perfume);
    
    let html = `
      <button class="fc-back-btn" onclick="window.fcGoBack()">Zurück</button>
    `;
    
    if (shopifyProduct) {
      html += `<div class="fc-subsection-title">Unsere inspirierte Version</div>`;
      html += '<div class="fc-grid-2col">';
      
      // RECOMMENDED: The found Shopify product
      html += renderProductCard(shopifyProduct, 'Empfohlen');
      
      // YOU MIGHT LIKE IT: Random product from your collection
      const otherProducts = fcProducts.filter(p => p.id !== shopifyProduct.id);
      if (otherProducts.length > 0) {
        const randomProduct = otherProducts[Math.floor(Math.random() * otherProducts.length)];
        html += renderProductCard(randomProduct, 'Das könnte dir auch gefallen');
      }
      
      html += '</div>';
    } else {
      // Fallback - show random products
      html += `<div class="fc-subsection-title">Das könnte dir gefallen</div>`;
      html += '<div class="fc-grid-2col">';
      
      const randomProducts = [...fcProducts].sort(() => 0.5 - Math.random()).slice(0, 2);
      if (randomProducts.length >= 1) {
        html += renderProductCard(randomProducts[0], 'Empfohlen');
      }
      if (randomProducts.length >= 2) {
        html += renderProductCard(randomProducts[1], 'Das könnte dir auch gefallen');
      }
      
      html += '</div>';
    }
    
    // Show catalog perfume details
    html += `
      <div class="fc-detailed-view">
        <div class="fc-detailed-header">
          <img class="fc-detailed-image" src="${perfume.image}" 
               alt="${perfume.name}"
               loading="lazy"
               onerror="this.src='https://via.placeholder.com/110x110?text=Parfum'">
          <div>
            <div class="fc-detailed-name">${perfume.name}</div>
            <div class="fc-detailed-brand">${perfume.brand}</div>
            ${perfume.year ? `<div class="fc-detailed-meta">${perfume.year}</div>` : ''}
            ${perfume.gender ? `<div class="fc-detailed-meta">${perfume.gender}</div>` : ''}
          </div>
        </div>

        <div class="fc-notes-section">
          ${perfume.top_notes ? `
            <div class="fc-note-category">
              <h4>KOPFNOTEN</h4>
              <div class="fc-note-tags">
                ${perfume.top_notes.map(note => `<span class="fc-note-tag">${note}</span>`).join('')}
              </div>
            </div>
          ` : ''}
          
          ${perfume.heart_notes ? `
            <div class="fc-note-category">
              <h4>HERZNOTEN</h4>
              <div class="fc-note-tags">
                ${perfume.heart_notes.map(note => `<span class="fc-note-tag">${note}</span>`).join('')}
              </div>
            </div>
          ` : ''}
          
          ${perfume.base_notes ? `
            <div class="fc-note-category">
              <h4>BASISNOTEN</h4>
              <div class="fc-note-tags">
                ${perfume.base_notes.map(note => `<span class="fc-note-tag">${note}</span>`).join('')}
              </div>
            </div>
          ` : ''}
        </div>
        
        <div class="fc-description">
          ${perfume.description}
        </div>
      </div>
    `;
    
    content.innerHTML = html;
  };

  // ===== FIND SHOPIFY PRODUCT BY CATALOG PERFUME - WITH HARDCODED MAPPINGS =====
  function findShopifyProductByCatalogPerfume(perfume) {
    if (!perfume || !fcProducts.length) return null;
    
    console.log('🔍 Finde Shopify-Produkt für Katalog-Parfum:', perfume.name);
    
    // ===== HARDCODED DIRECT MAPPINGS FOR KNOWN PRODUCTS =====
    const hardcodedMappings = [
      // DIOR
      {
        catalogNames: ['miss dior le parfum', 'miss dior'],
        productNumber: '196',
        variantId: 47306572714242, // Replace with actual variant ID
        expectedTitle: 'Riecht wie... Miss Dior Le Parfum - No. 196',
        imageUrl: 'https://magicperfume.co/cdn/shop/files/Christian_Dior_Miss_Dior_Le_Parfum.jpg?v=1770293457',
        url: 'https://magicperfume.co/products/magic-perfume-no-196w',
        price: 1199
      },
      // PACO RABANNE
      {
        catalogNames: ['one million lucky', '1 million lucky', 'million lucky', 'lucky one million'],
        productNumber: '280',
        variantId: 47306572386562,
        expectedTitle: 'Riecht wie... One Million Lucky - No. 280',
        imageUrl: 'https://magicperfume.co/cdn/shop/files/One_Million_Lucky.webp?v=1772135132',
        url: 'https://magicperfume.co/products/magic-perfume-no-280m',
        price: 1199
      },
      {
        catalogNames: ['one million', '1 million', 'million', 'paco rabanne one million'],
        productNumber: '275',
        variantId: 47306572353794,
        expectedTitle: 'Riecht wie… One Million - No. 275',
        imageUrl: 'https://magicperfume.co/cdn/shop/files/Paco_Rabanne_One_Million.png?v=1770217929',
        url: 'https://magicperfume.co/products/magic-perfume-no-275m',
        price: 1199
      },
      {
        catalogNames: ['one million elixir', 'million elixir', 'elixir'],
        productNumber: '334',
        variantId: 47306572419330,
        expectedTitle: 'Riecht wie... One Million Elixir - No. 334',
        imageUrl: 'https://magicperfume.co/cdn/shop/files/Paco_Rabanne_One_Million_Elixir.png?v=1770579856',
        url: 'https://magicperfume.co/products/magic-perfume-no-334m',
        price: 1199
      },
      {
        catalogNames: ['lady million', 'lady milion', 'lady'],
        productNumber: '023',
        variantId: 47306572452098,
        expectedTitle: 'Riecht wie... LADY MILION - No. 023',
        imageUrl: 'https://magicperfume.co/cdn/shop/files/Paco_Rabanne_Lady_Million.png?v=1770218839',
        url: 'https://magicperfume.co/products/magic-perfume-no-023w',
        price: 1199
      },
      {
        catalogNames: ['fame', 'paco rabanne fame'],
        productNumber: '498',
        variantId: 47306572484866,
        expectedTitle: 'Riecht wie... Fame - No. 498',
        imageUrl: 'https://magicperfume.co/cdn/shop/files/Paco_Rabanne_Fame.png?v=1770285610',
        url: 'https://magicperfume.co/products/magic-perfume-no-498w',
        price: 1199
      },
      {
        catalogNames: ['invictus', 'paco rabanne invictus'],
        productNumber: '228',
        variantId: 47306572517634,
        expectedTitle: 'Riecht wie... Invictus - No. 228',
        imageUrl: 'https://magicperfume.co/cdn/shop/files/Paco_Rabanne_Invictus.png?v=1770286917',
        url: 'https://magicperfume.co/products/magic-perfume-no-228m',
        price: 1199
      },
      {
        catalogNames: ['invictus victory', 'victory'],
        productNumber: '303',
        variantId: 47306572550402,
        expectedTitle: 'Riecht wie... Invictus Victory - No. 303',
        imageUrl: 'https://magicperfume.co/cdn/shop/files/Invictus_Victory.webp?v=1772136784',
        url: 'https://magicperfume.co/products/magic-perfume-no-303m',
        price: 1199
      },
      {
        catalogNames: ['phantom', 'paco rabanne phantom'],
        productNumber: '399',
        variantId: 47306572583170,
        expectedTitle: 'Riecht wie... Phantom - No. 399',
        imageUrl: 'https://magicperfume.co/cdn/shop/files/Paco_Rabanne_Phantom.png?v=1770580078',
        url: 'https://magicperfume.co/products/magic-perfume-no-399m',
        price: 1199
      },
      {
        catalogNames: ['pure xs', 'xs', 'pure xs for her', 'pure'],
        productNumber: '050',
        variantId: 47306572615938,
        expectedTitle: 'Riecht wie... Pure XS For Her - No. 050',
        imageUrl: 'https://magicperfume.co/cdn/shop/files/Paco_Rabanne_XS_For_Her.jpg?v=1772105675',
        url: 'https://magicperfume.co/products/magic-perfume-no-050w',
        price: 1199
      },
      {
        catalogNames: ['olympea', 'olympea intense', 'intense'],
        productNumber: '095',
        variantId: 47306572648706,
        expectedTitle: 'Riecht wie... Olympea Intense - No. 095',
        imageUrl: 'https://magicperfume.co/cdn/shop/files/Paco_Rabanne_Olympea_INTENSE.jpg?v=1772108016',
        url: 'https://magicperfume.co/products/magic-perfume-no-095w',
        price: 1199
      },
      {
        catalogNames: ['ultraviolet', 'ultra violet'],
        productNumber: '480',
        variantId: 47306572681474,
        expectedTitle: 'Riecht wie... Ultraviolet - No. 480',
        imageUrl: 'https://magicperfume.co/cdn/shop/files/Ultraviolet.webp?v=1772141928',
        url: 'https://magicperfume.co/products/magic-perfume-no-480w',
        price: 1199
      },
      
      // ARMANI
      {
        catalogNames: ['armani code', 'code armani', 'code men'],
        productNumber: '260',
        variantId: 47306572714242,
        expectedTitle: 'Riecht wie... Armani Code - No. 260',
        imageUrl: 'https://magicperfume.co/cdn/shop/files/Giorgio_Armani_Armani_Code.png?v=1770220395',
        url: 'https://magicperfume.co/products/magic-perfume-no-260m',
        price: 1199
      },
      {
        catalogNames: ['code for woman', 'armani code woman', 'code woman', 'code women'],
        productNumber: '135',
        variantId: 47306572747010,
        expectedTitle: 'Riecht wie... Code for Woman - No. 135',
        imageUrl: 'https://magicperfume.co/cdn/shop/files/Giorgio_Armani_Code_for_Woman.jpg?v=1771252264',
        url: 'https://magicperfume.co/products/magic-perfume-no-135w',
        price: 1199
      },
      
      // CHANEL
      {
        catalogNames: ['musc noir', 'narciso rodriguez musc noir', 'musc noir narciso rodriguez'],
        productNumber: '457',
        variantId: 47306571829506,
        expectedTitle: 'Riecht wie... Musc Noir For Her - No. 457',
        imageUrl: 'https://magicperfume.co/cdn/shop/files/Narciso_Rodriguez_Musc_Noir_For_Her.png?v=1770292273',
        url: 'https://magicperfume.co/products/magic-perfume-no-457w',
        price: 1199
      },
      {
        catalogNames: ['no. 5', 'chanel no. 5', 'no 5 chanel', 'chanel no5'],
        productNumber: '077',
        variantId: 47306570944770,
        expectedTitle: 'Riecht wie... No.5 - No. 077',
        imageUrl: 'https://magicperfume.co/cdn/shop/files/Chanel.jpg?v=1771891018',
        url: 'https://magicperfume.co/products/magic-perfume-no-077w',
        price: 1199
      },
      {
        catalogNames: ['bleu de chanel', 'bleu', 'chanel bleu', 'blue chanel'],
        productNumber: '252',
        variantId: 47306572321026,
        expectedTitle: 'Riecht wie... Bleu - No. 252',
        imageUrl: 'https://magicperfume.co/cdn/shop/files/Chanel_Bleud.png?v=1770283838',
        url: 'https://magicperfume.co/products/magic-perfume-no-252m',
        price: 1199
      }
    ];
    
    const perfumeLower = perfume.name.toLowerCase();
    
    // Check hardcoded mappings first
    for (const mapping of hardcodedMappings) {
      for (const catalogName of mapping.catalogNames) {
        if (perfumeLower.includes(catalogName)) {
          console.log(`🎯 Feste Zuordnung für "${catalogName}" -> Produkt #${mapping.productNumber}`);
          
          // First try to find existing product
          let match = fcProducts.find(p => {
            const pNumber = extractProductNumber(p.title);
            return pNumber === mapping.productNumber;
          });
          
          if (match) {
            console.log(`✅ Vorhandenes Produkt gefunden:`, match.title);
            return match;
          }
          
          // If not found, create a temporary product with the correct data
          console.log(`⚠️ Erstelle temporäres Produkt für #${mapping.productNumber}`);
          
          return {
            id: `temp-${mapping.productNumber}`,
            title: mapping.expectedTitle,
            image: mapping.imageUrl,
            url: mapping.url,
            variant: mapping.variantId,
            variantSKU: `${mapping.productNumber}${mapping.productNumber === '252' ? 'm' : 'w'}03`,
            price: mapping.price,
            description: mapping.expectedTitle
          };
        }
      }
    }
    
    // If no hardcoded match, try number-based search
    if (perfume.productNumbers && perfume.productNumbers.length > 0) {
      for (const num of perfume.productNumbers) {
        const match = fcProducts.find(p => {
          const pNumber = extractProductNumber(p.title);
          return pNumber === num;
        });
        if (match) {
          console.log(`✅ Gefunden über Produktnummer ${num}:`, match.title);
          return match;
        }
      }
    }
    
    console.log('❌ Keine Übereinstimmung gefunden für:', perfume.name);
    return null;
  }

  // ===== RENDER PRODUCT CARD WITH NUMERIC VARIANT ID =====
  function renderProductCard(product, badge) {
    const priceFormatted = product.price ? '€' + (product.price / 100).toFixed(2) : '€11.99';
    
    let displayNumber = '';
    let displayName = product.title;
    
    const numberMatch = product.title.match(/No\.?\s*(\d+)/i);
    if (numberMatch) {
      displayNumber = numberMatch[0];
    }
    
    const inspiredMatch = product.title.match(/Riecht wie\.\.\. (.*)/i);
    if (inspiredMatch) {
      displayName = inspiredMatch[1];
    }
    
    // Use the numeric variant ID (NOT the SKU)
    const variantId = product.variant;
    
    return `
      <div class="fc-product-card">
        <div class="fc-badge">${badge}</div>
        <div class="fc-product-inner">
          <img class="fc-product-image" src="${product.image}" alt="${product.title}" loading="lazy">
          <div class="fc-product-title">${displayNumber ? displayNumber + ' - ' : ''}${displayName}</div>
          <div class="fc-product-price">${priceFormatted}</div>
          <button class="fc-add-to-cart" onclick="event.stopPropagation(); window.fcAddToCart(${variantId})">In den Warenkorb</button>
          <div>
            <span class="fc-view-link" onclick="event.stopPropagation(); window.fcViewDetails('${product.url}')">Details ansehen</span>
          </div>
        </div>
      </div>
    `;
  }

  // ===== RENDER YOUR PRODUCT CARD (ALIAS) =====
  function renderYourProductCard(product, badge) {
    return renderProductCard(product, badge);
  }

  // ===== RENDER HARDCODED PRODUCT =====
  function renderHardcodedProduct(product, badge) {
    const priceFormatted = product.price ? '€' + (product.price / 100).toFixed(2) : '€11.99';
    
    return `
      <div class="fc-product-card">
        <div class="fc-badge">${badge}</div>
        <div class="fc-product-inner">
          <img class="fc-product-image" src="${product.image}" alt="${product.title}" loading="lazy">
          <div class="fc-product-title">${product.title}</div>
          <div class="fc-product-price">${priceFormatted}</div>
          <button class="fc-add-to-cart" onclick="event.stopPropagation(); window.fcAddToCart(${product.variant})">In den Warenkorb</button>
          <div>
            <span class="fc-view-link" onclick="event.stopPropagation(); window.fcViewDetails('${product.url}')">Details ansehen</span>
          </div>
        </div>
      </div>
    `;
  }

  // ===== INITIALIZE YOUR SHOPIFY PRODUCTS WITH CORRECT VARIANT IDS AND IMAGES =====
  function fcInitProducts() {
    fcProducts = [
      {% for product in collections.all.products %}
        {
          id: {{ product.id | json }},
          title: {{ product.title | json }},
          image: {{ product.featured_image | img_url: '400x' | json }},
          url: {{ product.url | json }},
          variant: {{ product.variants.first.id | json }},
          variantSKU: {{ product.variants.first.sku | json }},
          price: {{ product.price | json }},
          comparePrice: {{ product.compare_at_price | json }},
          description: {{ product.description | strip_html | json }},
          metafields: {
            perfume: {
              top_notes: {{ product.metafields.perfume.top_notes | default: '' | json }},
              middle_notes: {{ product.metafields.perfume.middle_notes | default: '' | json }},
              base_notes: {{ product.metafields.perfume.base_notes | default: '' | json }}
            },
            custom: {
              top_notes: {{ product.metafields.custom.top_notes | default: '' | json }},
              middle_notes: {{ product.metafields.custom.middle_notes | default: '' | json }},
              base_notes: {{ product.metafields.custom.base_notes | default: '' | json }},
              nuta: {{ product.metafields.custom.nuta | default: '' | json }}
            }
          }
        }{% unless forloop.last %},{% endunless %}
      {% endfor %}
    ];
    
    console.log('✓ Geladen', fcProducts.length, 'Produkte von Shopify');
    
    // Log all product numbers for debugging
    console.log('📋 Produktnummern in fcProducts:');
    fcProducts.forEach(p => {
      const number = extractProductNumber(p.title);
      console.log(`   ${p.title} -> Nummer: ${number}, Variant ID: ${p.variant}`);
    });
  }

  // ===== SHOW NOTIFICATION =====
  function showNotification(message) {
    let notification = document.querySelector('.fc-notification');
    if (!notification) {
      notification = document.createElement('div');
      notification.className = 'fc-notification';
      document.body.appendChild(notification);
    }
    
    notification.textContent = message;
    notification.classList.add('show');
    
    setTimeout(() => {
      notification.classList.remove('show');
    }, 2000);
  }

  // ===== UPDATE OFFER DISPLAY =====
  function updateOfferDisplay(itemCount) {
    if (!offerStatus || !offerProgress || !offerMessage || !offerSubtext) return;
    
    const percentage = Math.min((itemCount / 3) * 100, 100);
    offerProgress.style.width = percentage + '%';
    
    if (itemCount >= 3) {
      offerStatus.textContent = '🎉 GRATIS!';
      offerSubtext.textContent = 'Du qualifizierst dich für ein gratis Geschenk!';
      offerMessage.textContent = '🎉 Du qualifizierst dich für ein GRATIS Geschenk!';
      offerBanner.classList.add('qualified');
    } else {
      const needed = 3 - itemCount;
      offerStatus.textContent = `${itemCount}/3`;
      offerSubtext.textContent = `Füge ${needed} mehr hinzu für ein gratis Geschenk`;
      offerMessage.textContent = `Füge ${needed} mehr Artikel hinzu für ein gratis Geschenk`;
      offerBanner.classList.remove('qualified');
    }
  }

  // ===== TOGGLE POPUP =====
  window.fcToggleSearch = function() {
    if (searchPopup.classList.contains('active')) {
      searchPopup.classList.remove('active');
    } else {
      searchPopup.classList.add('active');
      searchInput.focus();
      fetchCartAndUpdate();
    }
  };

  // ===== FETCH CART AND UPDATE COUNT =====
  function fetchCartAndUpdate() {
    return fetch('/cart.js')
      .then(response => response.json())
      .then(cart => {
        document.querySelectorAll('.cart-count, [data-cart-count]').forEach(el => {
          if (el) el.textContent = cart.item_count;
        });
        updateOfferDisplay(cart.item_count);
        return cart;
      })
      .catch(error => {
        console.error('Fehler beim Abrufen des Warenkorbs:', error);
        return { item_count: 0 };
      });
  }

  // ===== ADD TO CART - MODIFIED TO NOT CLOSE POPUP =====
  window.fcAddToCart = function(variantId) {
    const btn = event.target;
    const originalText = btn.innerText;
    const originalBackground = btn.style.background;
    
    btn.innerText = 'Wird hinzugefügt...';
    btn.disabled = true;
    
    console.log('Füge zum Warenkorb hinzu mit Variante:', variantId);
    
    fetch('/cart/add.js', {
      method: 'POST',
      headers: { 
        'Content-Type': 'application/json',
        'X-Requested-With': 'XMLHttpRequest'
      },
      body: JSON.stringify({ 
        id: variantId, 
        quantity: 1 
      })
    })
    .then(response => response.json())
    .then(data => {
      btn.innerText = '✓ Hinzugefügt!';
      btn.style.background = '#2E7D32';
      
      fetchCartAndUpdate().then(cart => {
        showNotification(`✓ Hinzugefügt! ${cart.item_count}/3 Artikel für gratis Geschenk`);
      });
      
      setTimeout(() => {
        btn.innerText = originalText;
        btn.style.background = originalBackground || '#C41E3A';
        btn.disabled = false;
      }, 2000);
    })
    .catch(error => {
      console.error('Fehler beim Hinzufügen zum Warenkorb:', error);
      btn.innerText = 'Fehler';
      btn.style.background = '#c62828';
      
      setTimeout(() => {
        btn.innerText = originalText;
        btn.style.background = originalBackground || '#C41E3A';
        btn.disabled = false;
      }, 1500);
    });
    
    // IMPORTANT: Prevent event propagation to keep popup open
    event.stopPropagation();
    return false;
  };

  // ===== VIEW DETAILS =====
  window.fcViewDetails = function(url) {
    window.location.href = url;
  };

  // ===== GO BACK =====
  window.fcGoBack = function() {
    currentView = 'search';
    if (currentSearchQuery.length >= 1) {
      performSearch(currentSearchQuery);
    } else {
      steps.style.display = 'block';
      content.innerHTML = '';
    }
  };

  // ===== PERFORM SEARCH =====
  function performSearch(query) {
    currentSearchQuery = query;
    content.innerHTML = '<div class="fc-loading">Suche läuft...</div>';
    
    setTimeout(() => {
      // Search in catalog with hardcoded matching (NO FUZZY MATCHING)
      const catalogResults = advancedCatalogSearch(query);
      
      // Search in your products with advanced fuzzy matching
      const yourResults = searchProducts(query);
      
      // Search in hardcoded products as fallback
      const hardcodedResults = searchHardcodedProducts(query);
      
      let html = '';
      
      // Show catalog results (these are now hardcoded and accurate)
      if (catalogResults.length > 0) {
        html += renderCatalogPerfumes(catalogResults, query);
      }
      
      // Show your products
      if (yourResults.length > 0) {
        html += `<div class="fc-section-title">Unsere Produkte (${yourResults.length})</div>`;
        
        yourResults.slice(0, 6).forEach(match => {
          const p = match.product;
          const priceFormatted = p.price ? '€' + (p.price / 100).toFixed(2) : '€11.99';
          
          html += `
            <div class="fc-real-perfume-item" onclick="window.fcViewDetails('${p.url}')">
              <img class="fc-real-perfume-image" src="${p.image}" alt="${p.title}" loading="lazy">
              <div class="fc-real-perfume-info">
                <div class="fc-real-perfume-name">${p.title}</div>
                <div class="fc-real-perfume-year">${priceFormatted}</div>
              </div>
            </div>
          `;
        });
      }
      
      // Show hardcoded results if no other results
      if (catalogResults.length === 0 && yourResults.length === 0 && hardcodedResults.length > 0) {
        html += `<div class="fc-section-title">Empfohlene Produkte (${hardcodedResults.length})</div>`;
        
        hardcodedResults.slice(0, 6).forEach(match => {
          const p = match.product;
          const priceFormatted = '€' + (p.price / 100).toFixed(2);
          
          html += `
            <div class="fc-real-perfume-item" onclick="window.fcViewDetails('${p.url}')">
              <img class="fc-real-perfume-image" src="${p.image}" alt="${p.title}" loading="lazy">
              <div class="fc-real-perfume-info">
                <div class="fc-real-perfume-name">${p.title}</div>
                <div class="fc-real-perfume-year">${priceFormatted}</div>
              </div>
            </div>
          `;
        });
      }
      
      // If no results at all, show suggestions
      if (catalogResults.length === 0 && yourResults.length === 0 && hardcodedResults.length === 0) {
        html = '<div class="fc-no-results">Keine Produkte gefunden für "' + query + '"</div>';
        html += renderSuggestions();
      }
      
      content.innerHTML = html;
    }, 300);
  }

  // ===== SEARCH HANDLER =====
  function fcHandleSearch() {
    const query = searchInput.value.trim();
    
    if (query.length < 1) {
      if (currentView !== 'detail') {
        steps.style.display = 'block';
        content.innerHTML = '';
      }
      return;
    }
    
    steps.style.display = 'none';
    currentView = 'search';
    
    clearTimeout(searchTimer);
    searchTimer = setTimeout(() => performSearch(query), 300);
  }

  // ===== INITIALIZE =====
  document.addEventListener('DOMContentLoaded', function() {
    fcInitProducts();
    combineCatalogs();
    searchInput.addEventListener('keyup', fcHandleSearch);
    
    clearSearch.addEventListener('click', function() {
      searchInput.value = '';
      searchInput.focus();
      steps.style.display = 'block';
      content.innerHTML = '';
      currentView = 'search';
    });
    
    setTimeout(fetchCartAndUpdate, 500);
    
    document.addEventListener('click', function(event) {
      if (!searchPopup.contains(event.target) && !stickyButton.contains(event.target) && searchPopup.classList.contains('active')) {
        searchPopup.classList.remove('active');
      }
    });
    
    document.addEventListener('keydown', function(e) {
      if (e.key === 'Escape' && searchPopup.classList.contains('active')) {
        searchPopup.classList.remove('active');
      }
    });
    
    searchPopup.addEventListener('click', function(e) {
      e.stopPropagation();
    });
  });

})(); 
</script>